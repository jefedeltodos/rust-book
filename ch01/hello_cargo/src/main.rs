fn main() {
    println!("Hello, world!");
}

// `cargo build` to build
// `cargo run` to build and run the executable
// `cargo check` to check that the project will build, but does not output an executable.
// `cargo build --release` to build for production (longer compile) 